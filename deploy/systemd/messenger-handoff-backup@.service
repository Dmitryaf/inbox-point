[Unit]
Description=Create and verify Messenger Handoff external backup for %i
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
WorkingDirectory=/opt/messenger-handoff-%i
ExecStart=/usr/bin/docker compose -p %i --env-file .env --profile operations run --rm backup
